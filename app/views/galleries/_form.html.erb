
 <div class="navbar navbar-fixed-top">
    <div class="navbar-inner">
      <div class="container">
        <a class="btn btn-navbar" data-toggle="collapse" data-target=".nav-collapse">
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
        </a>
        <a class="brand" href="#">Chocochomp admin</a>
        <div class="nav-collapse">
          <ul class="nav">
            <li><%= link_to "Portfolios", portfolios_path %></li>
            <li><%= link_to "Galleries", galleries_path %></li>
            <li><%= link_to "Arts", arts_path %></li>
          </ul>
        </div>
      </div>
    </div>
  </div>

<%= form_for @gallery, :html => {:multipart=>true, :class => 'form-horizontal' } do |f| %>
  <div class="control-group">
    <%= f.label :title, :class => 'control-label' %>
    <div class="controls">
      <%= f.text_field :title, :class => 'text_field' %>
    </div>
  </div>
  <div class="control-group">
    <%= f.label :subtitle, :class => 'control-label' %>
    <div class="controls">
      <%= f.text_field :subtitle, :class => 'text_field' %>
    </div>
  </div>
  <div class="control-group">
    <%= f.label :likes, :class => 'control-label' %>
    <div class="controls">
      <%= f.number_field :likes, :class => 'number_field' %>
    </div>
  </div>
  <div class="control-group">
    <%= f.label :work, :class => 'control-label' %>
    <div class="controls">
      <%= f.text_field :work, :class => 'text_field' %>
    </div>
  </div>
  <div class="control-group">
    <%= f.label :thumbnail, :class => 'control-label' %>
    <div class="controls">
      <%= f.file_field :thumbnail, :class => 'file_field' %>
    </div>
  </div>
  <div class="control-group">
    <%= f.label :portfolio_id, :class => 'control-label' %>
    <div class="controls">
      <%= collection_select(:gallery, :portfolio_id, Portfolio.all, :id, :title) %>
    </div>
  </div>

  <div class="form-actions">
    <%= f.submit nil, :class => 'btn btn-primary' %>
    <%= link_to t('.cancel', :default => t("helpers.links.cancel")),
                galleries_path, :class => 'btn' %>
  </div>
<% end %>
